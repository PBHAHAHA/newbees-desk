'use client';

import React from 'react';

export const NavPanel = ({ children }: { children: React.ReactNode }) => {
  return <div className="w-[300px] bg-stone-800">{children}</div>;
};
// export const NavPanelItem =
